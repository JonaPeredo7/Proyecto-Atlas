import type{Cycle,Goal,PlanningOverview}from'../types/planning';async function req<T>(url:string,method='GET',body?:unknown){const r=await fetch(url,{method,credentials:'include',headers:{'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});if(!r.ok){const b=await r.json().catch(()=>null)as{message?:string}|null;throw new Error(b?.message??'No se pudo guardar el plan.')}return r.json()as Promise<T>}export const getPlanning=()=>req<PlanningOverview>('/api/planning');export const saveGoal=(body:unknown,id?:string)=>req<Goal>(id?`/api/planning/goals/${id}`:'/api/planning/goals',id?'PUT':'POST',body);export const saveCycle=(body:unknown,id?:string)=>req<Cycle>(id?`/api/planning/cycles/${id}`:'/api/planning/cycles',id?'PUT':'POST',body)
